% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\name{inject_deseq2}
\alias{inject_deseq2}
\title{Inject IRS size factors into a DESeq2 data set}
\usage{
inject_deseq2(dds, fit, overwrite = FALSE)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{fit}{An \code{irs_fit} object.}

\item{overwrite}{Replace existing DESeq2 size factors. Normalization-factor
matrices are never silently removed.}
}
\value{
The modified \code{DESeqDataSet}.
}
\description{
The original integer counts are retained. The injected size factors are the
geometric-mean-centered IRS reference totals, so DESeq2 normalized counts
are a common constant multiple of the IRS ratios. The returned data set can
be passed directly to \code{\link[DESeq2:DESeq]{DESeq2::DESeq()}}, which recognizes and uses the
pre-existing size factors. Do not call \code{DESeq2::estimateSizeFactors()} after
injection, because it would replace the IRS factors. Likewise, do not assign
a \code{DESeq2::normalizationFactors()} matrix after injection, because DESeq2
gives that matrix precedence over size factors.
}
