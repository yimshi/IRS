% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_irs}
\alias{fit_irs}
\title{Fit IRS normalization}
\usage{
fit_irs(
  counts,
  meta_dat,
  predictor,
  max_iter = 30,
  tolerance = 0.001,
  quantile_range = c(0, 1),
  p_threshold = 0.05,
  convergence_window = 3,
  verbose = TRUE,
  seed = NULL,
  kendall_transform = c("none", "log1p"),
  initial_ref = NULL,
  store_counts = FALSE
)
}
\arguments{
\item{counts}{Numeric count matrix with taxa in rows and samples in columns.
Unique taxon and sample names are required.}

\item{meta_dat}{Sample metadata. Matching row names are preferred; metadata
with default row names is assumed to already follow the count-column order.}

\item{predictor}{Name of the primary predictor in \code{meta_dat}.}

\item{max_iter, tolerance, quantile_range, p_threshold, convergence_window}{Arguments passed to \code{\link[=select_reference_irs]{select_reference_irs()}}.}

\item{verbose, seed, kendall_transform, initial_ref}{Arguments passed to
\code{\link[=select_reference_irs]{select_reference_irs()}}.}

\item{store_counts}{Store a copy of the count matrix in the fitted object.
The default is \code{FALSE} to keep large fits small.}
}
\value{
An object of class \code{irs_fit}.
}
\description{
Runs iterative reference selection and stores the final reference taxa,
reference totals, centered size factors, offsets, convergence information,
and sample diagnostics needed by downstream adapters.
}
