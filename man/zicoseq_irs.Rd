% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zicoseq.R
\name{zicoseq_irs}
\alias{zicoseq_irs}
\title{ZicoSeq with a fixed IRS reference set}
\usage{
zicoseq_irs(
  meta.dat,
  feature.dat,
  grp.name,
  adj.name = NULL,
  feature.dat.type = c("count", "proportion", "other"),
  prev.filter = 0,
  mean.abund.filter = 0,
  max.abund.filter = 0,
  min.prop = 0,
  is.winsor = FALSE,
  outlier.pct = 0.03,
  winsor.end = c("top", "bottom", "both"),
  is.post.sample = TRUE,
  post.sample.no = 25,
  link.func = list(function(x) sign(x) * abs(x)^0.5),
  stats.combine.func = max,
  perm.no = 99,
  strata = NULL,
  ref.pct = 0.5,
  stage.no = 1,
  excl.pct = 0,
  p.max = 500,
  is.fwer = FALSE,
  verbose = TRUE,
  return.feature.dat = TRUE,
  irs_fit = NULL,
  ref.features = NULL,
  zero_ref_action = c("error", "zicoseq_fallback")
)
}
\arguments{
\item{meta.dat}{Sample metadata with samples in rows.}

\item{feature.dat}{Integer count matrix with taxa in rows and samples in
columns.}

\item{grp.name}{Name of the variable of interest in \code{meta.dat}.}

\item{adj.name}{Optional adjustment-variable names.}

\item{feature.dat.type}{Retained for interface familiarity. IRS integration
currently supports count data only.}

\item{prev.filter, mean.abund.filter, max.abund.filter}{ZicoSeq feature filters.}

\item{min.prop}{Lower bound for sampled proportions.}

\item{is.winsor, outlier.pct, winsor.end}{ZicoSeq winsorization settings.}

\item{is.post.sample, post.sample.no}{Posterior-sampling settings.}

\item{link.func}{Link functions applied before testing.}

\item{stats.combine.func}{Function used to combine link-function statistics.}

\item{perm.no}{Number of permutations.}

\item{strata}{Optional permutation strata or a metadata column name.}

\item{ref.pct}{Retained for compatibility and ignored because IRS supplies
fixed references.}

\item{stage.no}{Retained for compatibility. Fixed IRS references require one
stage and values other than one are ignored with a warning.}

\item{excl.pct}{Retained for compatibility and ignored.}

\item{p.max}{Retained for compatibility and ignored.}

\item{is.fwer}{Compute permutation FWER-adjusted p-values.}

\item{verbose}{Print progress.}

\item{return.feature.dat}{Return the processed feature matrix.}

\item{irs_fit}{An optional \code{irs_fit} object. Supply exactly one of \code{irs_fit}
and \code{ref.features}.}

\item{ref.features}{Optional character vector of IRS reference-taxon names.}

\item{zero_ref_action}{Action when an original IRS reference total is zero.
The default stops; \code{"zicoseq_fallback"} uses half the smallest positive
divisor and records the affected samples.}
}
\value{
A list compatible with the principal fields returned by ZicoSeq,
plus an \code{irs} provenance and normalization component.
}
\description{
Runs the ZicoSeq count-data pipeline while replacing its internal reference
selection with taxa selected by IRS. Reference taxa are aligned by name and
their totals are computed from the original input counts before ZicoSeq
feature filtering or winsorization. The reference set is fixed for the
single testing stage; posterior sampling, covariate adjustment, link
functions, stratified permutation, and ZicoSeq-style multiplicity
adjustment are retained.
}
\details{
Supply the original integer count matrix rather than output from
\code{\link[=normalized_counts]{normalized_counts()}}. Use this wrapper in place of a subsequent call to
\code{GUniFrac::ZicoSeq()}: native ZicoSeq reference-selection controls are not
used because IRS supplies a fixed set. The original-count reference totals
remain fixed even when ZicoSeq later filters or winsorizes tested features.
The default \code{zero_ref_action = "error"} preserves the IRS denominator
definition; the fallback option changes zero denominators and is intended
only for an explicitly reported sensitivity analysis.

This function is derived from \code{GUniFrac::ZicoSeq()} version 1.9 under GPL-3.
}
